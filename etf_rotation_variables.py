# -*- coding: utf-8 -*-
"""
国金QMT ETF轮动策略 - 可配置变量
=================================
本文件由 ETF轮动策略.xml 自动生成，所有变量均可通过国金QMT配置面板绑定。
在国金QMT策略编辑器中，这些变量名会出现在右侧参数配置面板。
"""


# ==================================================
# 一、持仓与仓位管理
# ==================================================

# 最大持仓ETF数
# 说明: 持仓动量前N只ETF
MAX_HOLDINGS = 5  # 默认值: 5

# 单只仓位上限
# 说明: 单只ETF目标仓位上限，如0.12表示12%
TARGET_POSITION_PCT = 0.12  # 默认值: 0.12

# ETF资金上限
# 说明: ETF资金上限（元），0=不限制；策略只管理ETF池内资金，且持仓成本不超过此上限，市值可超过
MAX_ETF_CAPITAL = 0  # 默认值: 0

# ==================================================
# 二、动量计算参数
# ==================================================

# 长期动量天数
# 说明: 长期动量计算天数
M_DAYS_LONG = 60  # 默认值: 60

# 短期动量天数
# 说明: 短期动量计算天数
M_DAYS_SHORT = 20  # 默认值: 20

# R2权重
# 说明: R2惩罚权重，0=完全用R2惩罚(原公式)，1=完全不用R2，默认0.5
R2_WEIGHT = 0.5  # 默认值: 0.5

# ==================================================
# 三、买入过滤参数
# ==================================================

# 最低买入分数
# 说明: 最低买入分数（年化趋势收益门槛），低于此分不买。0=买所有正趋势ETF，0.05=只买≥5%的
MIN_BUY_SCORE = 0.0  # 默认值: 0.0

# 波动率上限
# 说明: 年化波动率上限，超过则排除
VOL_THRESHOLD = 0.45  # 默认值: 0.45

# ==================================================
# 四、调仓时间参数
# ==================================================

# 调仓星期
# 说明: 调仓星期，0=周一，1=周二，...，6=周日
REBALANCE_WEEKDAY = 2  # 默认值: 2

# 调仓时间
# 说明: 调仓时间(HH:MM)，日线周期可任意填
RUN_TIME = '09:31'  # 默认值: 09:31

# ==================================================
# 五、风控参数
# ==================================================

# ATR周期
# 说明: ATR计算周期
ATR_PERIOD = 14  # 默认值: 14

# ATR止损倍数
# 说明: ATR止损倍数
ATR_STOP_MULT = 3.0  # 默认值: 3.0

# 硬止损比例
# 说明: 硬止损比例，如-0.08表示亏损8%清仓
HARD_STOP_LOSS = -0.08  # 默认值: -0.08

# 移动止盈回撤
# 说明: 移动止盈回撤比例，如0.10表示回撤10%止盈
TRAILING_STOP_PCT = 0.1  # 默认值: 0.10

# 绝对止盈比例
# 说明: 绝对止盈比例，盈利达到后卖出一半
TAKE_PROFIT_RATIO = 0.2  # 默认值: 0.20

# ==================================================
# 六、算法单参数
# ==================================================

# 使用算法单
# 说明: True=使用algo_passorder算法拆单，False=普通下单
USE_ALGO_ORDER = True  # 默认值: True

# 算法单类型
# 说明: 算法单类型，0=普通交易，1=普通算法单，2=随机量交易
ALGO_ORDER_TYPE = 1  # 默认值: 1

# 算法单报价类型
# 说明: 报价类型，14=对手价（沪深两市通用），5=最新价，13=挂单价，44=对手方最优（仅深市），45=本方最优（仅深市）
ALGO_PRICE_TYPE = 14  # 默认值: 14

# 算法单超价比例
# 说明: 超价比例，0.0001=0.01%
ALGO_SUPER_PRICE_RATE = 0.0001  # 默认值: 0.0001

# 算法单量类型
# 说明: 量类型，10=目标量，11=目标剩余量，12=持仓数量，0~9=各档位盘口量
ALGO_VOLUME_TYPE = 10  # 默认值: 10

# 算法单量比例
# 说明: 每笔委托占剩余目标量的比例
ALGO_VOLUME_RATE = 0.2  # 默认值: 0.2

# 算法单单笔最小股数
# 说明: 单笔最小股数
ALGO_SINGLE_NUM_MIN = 100  # 默认值: 100

# 算法单单笔最大股数
# 说明: 单笔最大股数
ALGO_SINGLE_NUM_MAX = 100000  # 默认值: 100000

# 算法单报单间隔
# 说明: 报单间隔（秒）
ALGO_PLACE_ORDER_INTERVAL = 10  # 默认值: 10

# 算法单有效时间
# 说明: 算法单有效时间（秒）
ALGO_VALID_TIME_ELAPSE = 1800  # 默认值: 1800

# 算法单最大委托数
# 说明: 最大委托笔数
ALGO_MAX_ORDER_COUNT = 150  # 默认值: 150

# ==================================================
# 七、篮子组合交易参数
# ==================================================

# 使用篮子组合交易
# 说明: True=使用篮子组合交易（在组合任务面板聚合显示），False=单股算法单
USE_BASKET_TRADE = True  # 默认值: True

# 篮子名称前缀
# 说明: 篮子名称前缀，买入篮子名称为前缀_buy
BASKET_NAME_PREFIX = 'ETF轮动'  # 默认值: ETF轮动

# 买入篮子下单方式
# 说明: 买入篮子下单方式，2102=按组合权重（金额），2101=按组合数量
BASKET_ORDER_TYPE_BUY = 2102  # 默认值: 2102

# ==================================================
# 八、系统运行参数
# ==================================================

# 启用日志
# 说明: 是否输出策略日志
ENABLE_LOGGING = True  # 默认值: True

# 跳过历史bar
# 说明: 实盘建议开启：跳过历史回补bar，只处理当天；回测请关闭
SKIP_HISTORY_BAR = True  # 默认值: True

# 状态文件路径
# 说明: 状态持久化文件路径，支持相对路径或绝对路径
STATE_FILE_PATH = 'etf_rotation_state.json'  # 默认值: etf_rotation_state.json

# 在策略代码中可以直接使用上述变量名，例如：
# if USE_ALGO_ORDER: ...
